version: '3.4'
services:
  pg-rrule:
    container_name: pg-rrule
    image: postgres:11.4-alpine-dev
    ports:
      - 5433:5432
    volumes: 
      - .:/usr/src/app
    tty: true
    working_dir: /usr/src/app
